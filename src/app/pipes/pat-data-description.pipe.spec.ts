import { PatDataDescriptionPipe } from './pat-data-description.pipe';

describe('PatDataDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new PatDataDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
