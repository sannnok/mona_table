import { TableHeaderPipe } from './table-header.pipe';

describe('TableHeaderPipe', () => {
  it('create an instance', () => {
    const pipe = new TableHeaderPipe();
    expect(pipe).toBeTruthy();
  });
});
